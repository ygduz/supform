# Copy this file to .env and fill in your deployed backend URL.
# This URL is baked into the web bundle at build time.
# The web app at frontend/src/api/client.ts reads VITE_API_BASE_URL automatically.

VITE_API_BASE_URL=https://api.yourdomain.com
